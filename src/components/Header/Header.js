import React from 'react';
import './Header.css';

const Header = (props) => {
    return (
        <div className="header d-flex justify-content-between">
            Vedantu
        </div>
    );
};

export default Header;
